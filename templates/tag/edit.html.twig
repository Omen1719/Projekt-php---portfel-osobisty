{% include 'default/_form.html.twig' with {
    form: form,
    page_title: 'title.tag_edit'|trans({'%id%': tag.id|default('')}),
    submit_label: 'action.edit'|trans,
    back_to_list_path: url('tag_index'),
} only %}
