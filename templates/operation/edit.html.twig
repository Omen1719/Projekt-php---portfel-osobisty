{% include 'default/_form.html.twig' with {
    form: form,
    page_title: 'title.operation_edit'|trans({'%id%': operation.id|default('')}),
    submit_label: 'action.edit'|trans,
    back_to_list_path: url('operation_index'),
} only %}
