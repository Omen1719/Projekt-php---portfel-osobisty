{% include 'default/_form.html.twig' with {
    form: form,
    page_title: 'title.wallet_delete'|trans({'%id%': wallet.id|default('')}),
    submit_label: 'action.delete'|trans,
    back_to_list_path: url('wallet_index'),
} only %}
