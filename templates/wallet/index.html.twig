{% extends 'base.html.twig' %}

{% block title %}{{ 'title.wallet_list'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'title.wallet_list'|trans }}</h1>

    <p>
        <a href="{{ url('wallet_create') }}" class="btn btn-primary" title="{{ 'action.create'|trans }}">
            {{ 'action.create'|trans }}
        </a>
    </p>

    {% if pagination is defined and pagination.items|length %}
        <div class="navigation text-center">{{ knp_pagination_render(pagination) }}</div>

        <table class="table table-striped">
            <thead>
            <tr>
                <th>{{ knp_pagination_sortable(pagination, 'label.id'|trans, 'wallet.id') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.title'|trans, 'wallet.title') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.wallet_type'|trans, 'wallet.type') }}</th>
                <th>{{ knp_pagination_sortable(pagination, 'label.updated_at'|trans, 'wallet.updatedAt') }}</th>
                <th>{{ 'label.actions'|trans }}</th>
            </tr>
            </thead>
            <tbody>
            {% for wallet in pagination.items %}
                <tr>
                    <td>{{ wallet.id }}</td>
                    <td>{{ wallet.title }}</td>
                    <td>{{ ('label.wallet_' ~ wallet.type)|trans }}</td>
                    <td>{{ wallet.updatedAt|format_date('long') }}</td>
                    <td>
                        {% include 'default/_row_actions.html.twig' with {route_prefix: 'wallet', id: wallet.id} only %}
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <div class="navigation text-center">{{ knp_pagination_render(pagination) }}</div>
    {% else %}
        <p>{{ 'message.empty_list'|trans }}</p>
    {% endif %}
{% endblock %}
