{% include 'default/_form.html.twig' with {
    form: form,
    page_title: 'title.user_password'|trans({'%id%': user.id|default('')}),
    submit_label: 'action.set_password'|trans,
    back_to_list_path: url('admin_user_index'),
} only %}
